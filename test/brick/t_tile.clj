(ns brick.t_tile
  (:use midje.sweet)
  (:use [brick.core]))

(fact "load tiles correctly"
      ; number of tiles
      ; edge rounding?
      ; samples
      )

(fact "with-tiles correctly binds tiles"
      ; binging
      ; nested binding
      )

(fact "with-dictionary binds dictionary correctly"
      ; binding
      ; nested binding
      )

(fact "tile fuction returns correct tile"
      ; binding
      ; nested binding
      )

(fact "Image tile creates a tile drawer.")
